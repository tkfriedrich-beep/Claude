# Daily Plan

Sequences due commitments first, then one deep-work block per active project, around your fixed
events (demo calendar until Google is connected — always labeled). Everything it proposes is a
draft; it never books or moves calendar entries (`google.calendar.create_event` is not in its
allowlist).
